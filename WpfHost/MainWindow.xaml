<Window x:Class="WpfHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfHost"
        Title="C++ FAND" Background="Black" SizeToContent="WidthAndHeight" ResizeMode="CanMinimize"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom" Background="#FF202020" Foreground="#FFB0B0B0" FontSize="12">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="start…"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="Shift+Ins vložit · Ctrl+Shift+C kopie obrazovky · Ctrl+kolečko velikost písma"/>
            </StatusBarItem>
        </StatusBar>
        <Grid>
            <local:TerminalControl x:Name="Terminal"/>
            <Canvas x:Name="Overlay" IsHitTestVisible="True"/>
        </Grid>
    </DockPanel>
</Window>
